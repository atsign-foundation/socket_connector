# Run with `dart run ffigen --config ffigen.yaml`.
name: AtNativeSocketFlutterBindings
description: |
  Bindings for `src/at_native_socket_flutter.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/at_native_socket_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/at_native_socket_flutter.h'
  include-directives:
    - 'src/at_native_socket_flutter.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
